services:
  - type: web
    name: anti-gravity-engine
    env: python
    rootDir: anti-gravity-engine
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port $PORT --server.address 0.0.0.0
